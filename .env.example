# 对外监听地址。只允许本机访问时改为 127.0.0.1。
FINFORGE_HOST=0.0.0.0

# Web 入口端口；API 由同一入口的 /api 反向代理。
FINFORGE_PORT=8080

# compose.prod.yaml 使用的镜像标签，可填写 latest、版本号或 sha-* 标签。
FINFORGE_IMAGE_TAG=latest

TZ=Asia/Shanghai
